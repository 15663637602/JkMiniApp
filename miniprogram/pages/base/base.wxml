<view>{{msg}}</view>
<view wx:for="{{arr}}" wx:key="index">
  {{index}} {{item}}
</view>

<view wx:for="{{list}}" wx:key="index">
  {{item.name}}  {{item.age}}
</view>

<view wx:if="{{isAdmin}}">Jerry</view>
<view wx:else>No permit</view>
<view hidden="{{isHidden}}">hidden</view>
<!-- 如果频繁切换的话 用hidden更好， 如果运行时条件不变，用if-->

<view class="box" catchtap="onTapBoxHandler" data-id="1234">
  <view class="child" catchtap="onTapChildHandler"></view>
</view>

<button size="mini" bindtap="onTapHandler">点我加1</button> <!--bindtap会导致事件传播，点击子标签会传递到父标签-->
<view>{{cnt}}</view>